import 'package:flutter/material.dart';

class PatternContainer extends StatelessWidget {
  const PatternContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}